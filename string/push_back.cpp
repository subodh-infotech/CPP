#include <iostream>
#include <string>

using namespace std;

int main() {
    string str("hell");
    str.push_back('o'); // this function is used to append the specified character at the end of string.
    cout << endl << str;
    return 0;
}